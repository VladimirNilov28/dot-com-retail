spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
jwt:
  secret: ${JWT_SECRET}
dgs:
  graphql:
    introspection:
      enabled: false
    graphiql:
      enabled: false